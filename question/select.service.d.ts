import { FormControl } from '@angular/forms';
import { Select } from '.';
export declare class SelectService {
    static onChangeOption(htmlFormControl: HTMLInputElement, formControl: FormControl, question: Select): void;
}
